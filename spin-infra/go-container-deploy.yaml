apiVersion: apps/v1
kind: Deployment
metadata:
  name: spin-go-deployment
  labels:
    app: spin-go-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spin-go-app
  template:
    metadata:
      labels:
        app: spin-go-app
    spec:
      containers:
      - name: spin
        image: ghcr.io/fermyon/spin:v2.7.0
        args: [
          "up",
          "-k",
          "--listen",
          "0.0.0.0:8080",
          "-f",
          "10.164.178.1:5000/spin-go-server:latest"
          # "192.168.68.54:5000/spin-go-server:latest"
        ]
        ports:
        - containerPort: 8080
          name: http-0
---
apiVersion: v1
kind: Service
metadata:
  name: spin-go-server
spec:
  selector:
    app: spin-go-app
  ports:
    - name: http-0
      port: 80
      protocol: TCP
      targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spin-go-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: nuhakala.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: spin-go-server
            port:
              number: 80
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: spin-go-deploy-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: spin-go-deployment
  minReplicas: 1
  maxReplicas: 100
  metrics:
  - type: External
    external:
      metric:
        name: http_requests_per_second
      target:
        type: Value
        averageValue: 10
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 60
---
