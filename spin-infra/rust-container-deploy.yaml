apiVersion: apps/v1
kind: Deployment
metadata:
  name: spin-rust-deployment
  labels:
    app: spin-rust-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spin-rust-app
  template:
    metadata:
      labels:
        app: spin-rust-app
    spec:
      containers:
      - name: spin
        image: ghcr.io/fermyon/spin:v2.7.0
        args: [
          "up",
          "-k",
          "--listen",
          "0.0.0.0:8080",
          "-f",
          "10.164.178.1:5000/spin-rust-server:latest"
          # "192.168.68.54:5000/spin-rust-server:latest"
        ]
        ports:
        - containerPort: 8080
          name: http-0
---
apiVersion: v1
kind: Service
metadata:
  name: spin-rust-server
spec:
  selector:
    app: spin-rust-app
  ports:
    - name: http-0
      port: 80
      protocol: TCP
      targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spin-rust-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: nuhakala.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: spin-rust-server
            port:
              number: 80
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: spin-rust-deploy-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: spin-rust-deployment
  minReplicas: 1
  maxReplicas: 100
  metrics:
  - type: External
    external:
      metric:
        name: http_requests_per_second
      target:
        type: Value
        averageValue: 10
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 60
---
