apiVersion: v1
kind: Service
metadata:
  name: go-service
spec:
  selector:
    app.kubernetes.io/name: go-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
  # externalIPs: ["10.164.178.101"]
